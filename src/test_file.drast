//
// Drast Programming Language Examples
//

import io

enum Test {
    case Test = 10,
    case Test,
    case Test,
}

struct Hello {
    var test: Int
    let test: String
}

func multiply(first: Int, second: Int) -> Int {
    return first * second
}

func main(argc: Int, argv: String[]) -> Int {
    // print("Hello World")
    var sadfasdf: Float[]
    var test = 6 / 3 + 2 + 50 / (9 + 10)
    var test = (-5 - 3)
    var test = (test + 50)
    var number: Int = 50
    return 0
}
