use std
use token
use lexer

main, int
	path = arg 1
	source = readFile path
	lex = Lexer[source path]
	tokens = lex.lex
	for tok in tokens
		print tokenKindName[tok.kind]
		print ': '
		if tok.kind isne .Newline
			println tok.text

	// parser = Parser[tokens]


	return 0
