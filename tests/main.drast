//
// Drast Programming Language Tests
//

import io

func printf(string fmt, ... args)

private func main(): i64 || @(struct first_type) {
    var test = 30
    var test1: i64 = 40
    printf("Hello, world!")

    return 0
}
