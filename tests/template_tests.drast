//
// Drast Programming Language Tests
//

struct Pair : @(struct first_type, struct second_type) {
    private var first: first_type
    private var second: second_type

    func get_first(): first_type {
        return first
    }

    func get_second(): second_type {
        return second
    }

    func test_function(test: i32, test: string) || @(struct first_type) {
        
    }
}

struct Person {
    var name: string
    var age: i32
}

enum AgeGroup {
    Child,
    Teenager,
    Adult,
    Senior
}

struct Test {
    var john_doe: Pair@(Person, AgeGroup)

    john_doe.test_function@(Person)(1, "test")
}